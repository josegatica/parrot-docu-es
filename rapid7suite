aqui vamos team

1. wget https://downloads.metasploit.com/data/releases/metasploit-latest-linux-x64-installer.run

2. service postgresql start

3. update-rc.d postgresql enable
update-rc.d metasploit enable
update-rc.d nginx enable

4. su postgres
createuser root -P
createdb —owner=root msfdb
exit

5. msfconsole
db_status
db_rebuild_cache
load nexpose
exit

6. locate diagnostic_shell
sudo /opt/metasploit/diagnostic_shell
cd /opt/metasploit/apps/pro/ui
ruby script/createuser

root

detallemos

1. descargamos metasploit pro

2. Iniciamos servicio de postgresql

3. Dejamos los 3 servicios base automaticos al iniciar el sistema

4. Creamos usaurio root en la base de datos de metasploit

5. iniciamos la consola de metasploit verificamos que la bases de datos este arriba, la iniciamos y activamos neXpose

6. Creamos el usuario root para la interfaz web de Metasploit

En 6 puntos trate de darles una guia sencilla

En este punto ya podemos abrir la consola de metasploit

https://localhost:1337

Ahora lo mas importante la licencia

Vamos a esta direccion

http://r-7.co/trymsp

Usamos cualquier correo

Eliges para uso personal

puedes  volver a generar el # de licencias que gustes que incluye para 3 usuarios

disfrutemos mientras se pueda, no olviden abrir los puertos que ustedes definan durante la instalacion, y los que vayan a requerir para sus laboratorios, Urug puede entrar en mas detallen si  gustan

enjoy and care

...

Bien parceros

vamos ahora con neXpose

Este funciona mejor en windows

1. http://download2.rapid7.com/download/InsightVM/Rapid7Setup-Windows64.exe

2. Instalan y permiten trafico en el firewall de windows puertos y en su vps en cao de tenerlo sobre una

3. Prueban localmente

Puertos= 3790 metasploit, neXpose + Insigth VM 3780, 40814, 40815
